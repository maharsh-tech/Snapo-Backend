{
  "name": "image-hosting-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — Telegram-backed image hosting backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
